import { mockExperience } from '@/lib/sanity/mock';
import { IExperienceCard } from './ExperienceCard';

const base: IExperienceCard = {
  experience: mockExperience,
};

export const mockExperienceCardProps = {
  base,
};
