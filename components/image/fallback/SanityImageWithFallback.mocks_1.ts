import { ISanityImageWithFallback } from './SanityImageWithfallback';

const base: ISanityImageWithFallback = {
  image: {},
  src: '',
  alt: '',
};

export const mockSanityImageWithFallbackProps = {
  base,
};
